__pycache__/
*.pyc
*.tmp
.DS_Store
.venv/

# Published as a GitHub Release asset instead (no size ceiling, unlike a
# committed git blob). Local copies stay for debugging only.
data/snapshot.json
data/snapshot.json.gz

# Tournaments cache: published as a Release asset (like snapshot), not committed
data/tournaments.json
data/tournaments.json.gz
